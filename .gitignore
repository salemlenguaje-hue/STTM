# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/

# --- Entorno y Editores ---
.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/

# --- STTM: Seguridad y Datos Privados (Alineado a R14-15 / R20-15) ---
# Ignoramos bases de datos locales (se exportan canónicamente si hace falta)
data/*.db
data/*.sqlite

# Ignoramos claves criptográficas y secretos
*.key
*.pem
*.env
*.secret

# Ignoramos temporalidades de auditoría
auditorias/temp/

# --- Notas de estrategia local (con nombres de entidades) ---
data/estrategia/

# --- K-005: clave publica anclada (verificable por terceros) ---
!data/claves/sofia_publica.pem
